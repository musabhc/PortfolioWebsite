@model SocialMedia
@{
    ViewData["Title"] = "UpdateSocialMedia";
    Layout = "~/Views/Layout/Index.cshtml";
}

<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">Update Social Media</h4>
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Update Social Media</div>
                    </div>
                    <div class="card-body">
                        <form method="post" enctype="multipart/form-data">

                            <input type="hidden" asp-for="socialMediaId" />

                            <label>Title</label>
                            <input type="text" asp-for="title" class="form-control" />
                            <br />

                            <label>URL</label>
                            <textarea rows="4" class="form-control" asp-for="url"></textarea>
                            <br />

                            <label>Current Image</label>
                            <br />
                            <img src="@Model.icon" alt="Current Image" style="max-width: 200px; height: auto;" />
                            <br />

                            <br />
                            <label>New Image (optional)</label>
                            <input type="file" asp-for="icon" class="form-control" />
                            <br />



                            <button type="submit" class="btn btn-primary">Update Social Media</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

